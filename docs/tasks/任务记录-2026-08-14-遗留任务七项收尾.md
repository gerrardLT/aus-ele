# 任务记录-2026-08-14-遗留任务七项收尾（安全收紧+自助功能+自动化+测试债）

状态：已完成（随当日批次提交部署）

依据：用户指令「除了 5（支付）、9（法务措辞）、10（人工验收）其他都完成」。本轮完成 7 项遗留：

---

## 任务 1：server.py 旧告警端点鉴权收紧（P2 审计遗留）

1. server.py 新增 `_require_alerts_actor`（Bearer 全链校验，写操作要求 owner/admin）与 `_scoped_alert_workspace`（workspace 一致性）
2. 五个旧端点全部收紧：POST/GET /api/alerts/rules、GET states、GET delivery-logs、POST evaluate；创建时强制规则归属调用者 workspace（堵跨租户通知注入链）
3. admin_routes.py 的重复注册 GET /api/alerts/rules（实际生效入口）同样加鉴权
4. 前端 AlertRulesPage 列表请求携带令牌

## 任务 2：治理测试随机隔离改造（存量技术债）

1. test_org_governance_routes.py / test_oidc_auth_routes.py 整体重写：邮箱/域名/OIDC subject/client_id 全部随机后缀
2. test_org_governance.py：两个测试类的固定 ID/邮箱/邀请 token 随机化；修复 PG 外键约束（accepted_by_principal_id 先建 principal）；修复已过期的邀请日期（2026-05 → 2027）；domain_join_route 改用 DomainJoinRequest 体调用（签名修复）
3. **验证：三文件组合连跑两遍 43/43 ×2 全绿**（此前重复运行必失败）

## 任务 3：邀请接受端点限流

`check_invite_accept_rate_limit`：同一 invite_token+IP 10 分钟滑动窗口最多 10 次（429）；应用于 JSON body 版（account_routes）与 query 版（server.py）两个入口。

## 任务 4：修改密码自助功能

- access_control.change_principal_password（验旧改新，401 语义与登录一致，新密码 ≥8 位）
- POST /api/v1/account/password；AccountPage 总览加「修改密码」表单（两次确认+错误提示）

## 任务 6：OIDC SSO 开通准备

- LoginPage 加「企业 SSO 登录」折叠入口（组织 ID + google/microsoft，调 /api/auth/oidc/start 跳转）
- 新增 backend/scripts/enable_oidc.py：注册 OIDC provider（幂等，内置 Google/Microsoft issuer/discovery 默认值）
- 待用户动作：在 Google/Azure 注册应用拿 client_id/secret 后跑脚本即开通

## 任务 7：报告定时订阅推送

1. 新表 report_subscription（monthly 按日 / weekly 按星期，收件邮箱缺省用账户邮箱）
2. p2_routes 订阅 CRUD（仅本人管理自己的订阅，删除可由 owner/admin 代管）
3. services/report_scheduler.py：每日检查到期订阅 → 生成上月报告 → 邮件投递，SMTP 缺失/失败降级站内通知；无论如何落库一份保存报告；同日不重发（last_sent_at 保护）；月末钳制（31 日订阅在 30 天月份月末触发）
4. server.py 调度器加 report-subscription-daily 作业（默认 03:20，env 可调）
5. ReportsPage 加订阅表单与列表

## 任务 8：通知表自动清理

purge_expired_notifications（默认保留 90 天），通知列表查询时惰性触发（best-effort）。

## 验证

- 新增用例：改密流程（401/成功/新密码可登录）、限流（直调+端点 429）、订阅生命周期（422/创建/列表/权限删除）、到期投递+同日跳过+站内降级、通知清理——合计 100 用例全绿（p2 22 + account 25 + auth 14 + 治理三件 43）
- vite build 通过；funnelProperties 38/38
- server.py 本轮有受控修改（告警鉴权 + 调度作业 + 邀请限流），均为点位式小改动

## 剩余遗留

- P1-3 支付集成 + 硬配额阻断（用户决策跳过）
- 法务措辞人工过目（用户决策跳过）
- 全流程人工验收（用户决策跳过）
- OIDC 实际开通待用户提供 IdP 应用凭据
