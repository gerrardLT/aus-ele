[pytest]
# Register custom markers so pytest does not emit PytestUnknownMarkWarning.
# ``benchmark`` is used by tests/test_performance_benchmarks.py (requires the
# pytest-benchmark plugin; the module is currently xfail(run=False) until PG
# test fixtures are available).
markers =
    benchmark: performance benchmark tests (require the pytest-benchmark plugin)
