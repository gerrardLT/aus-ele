# 任务记录：V2 转正与信任组件落地

- 日期：2026-08-24
- 类型：治理转正 + 功能开发（承接同日《前端设计漂移修复》推迟项 1/2/3/5）
- 前置文档：`docs/tasks/任务记录-2026-08-24-前端设计漂移修复.md`
- 用户裁定：转正范围 = 文档治理 + 全部 6 个信任组件；apiBase.test.js 归入 vitest（零改动）

## 一、测试 runner 收口（apiBase.test.js 归属修正）

- 根因核实：CI/README/部署文档的 node --test glob 均为 `src/lib/*.test.js`（不含 src/test）；
  上次 317/318 的失败来自人工执行时手动追加了 `src/test/*.test.js` 段，裸 `node --test`
  的默认探测也会误拾取 vitest 文件
- `web/package.json`：新增权威脚本 `"test:node": "node --test src/lib/*.test.js"`（Node 22 原生支持 glob）
- 三处文档同步双 runner 边界（只加注释，未改命令本身，避开 ci.yml 并行冲突）：
  - `.github/workflows/ci.yml` L104 注释区
  - `README.md` Verification 段（含"禁用裸 node --test"说明）
  - `docs/deployment/CICD部署与回滚说明.md` 6.2 节
- `web/src/test/apiBase.test.js` 零改动，vitest 已自然接管（vite.config.js test.exclude 仅排除 src/lib）

## 二、DESIGN-v2 治理转正

| 动作 | 结果 |
|---|---|
| `docs/design/DESIGN-v2.md` version alpha → 1.0 | designmd lint 0 error / 9 warning（与基线持平） |
| v1 物理归档 | `git mv docs/design/DESIGN.md → docs/_archive/DESIGN-v1.md`（改名避免与根混淆）；内部链接改 `../design/DESIGN-v2.md`；superseded 块补归档去向 |
| token 双源合一 | `designmd export --format dtcg` 重生成 design_tokens.json（8804 字节）；校验：合法 JSON、含 Archivo、无 Inter、含 confidence token |
| 根 DESIGN.md | SSOT 声明补 v2 权威源指针 + 版本记录追加 v1.3 行（只追加，规避 42 行并行改动冲突） |

**design_tokens.json 结构差异说明**：export 产物顶层为 `$schema/$description/color/spacing/rounded/typography`
标准 DTCG 形状，不含 components 段（autonomy-dial-option 等组件 token 仅存在于 v2 frontmatter）；
旧手工版 506 行的 colorSpace/components/hex 自定义形状被覆盖。全仓无代码消费者，零破坏。

**历史引用处理**：docs/tasks ×3、docs/research ×1 仍引用旧路径——不可变历史日志，不回改
（代码/AGENTS.md/测试零路径引用，已 grep 复核）。

## 三、6 个信任组件落地（web/src/components/agent/ 新目录）

| 组件 | 模式 | 接线点 |
|---|---|---|
| ConfidenceBadge.jsx | Confidence Signal | 替换 ReportView 综合建议区纯文本置信度 |
| IntentPreview.jsx | Intent Preview | 导出 PDF 前的遮罩摩擦点（AgentPage 调用侧包裹，未编辑 ExportPreviewModal） |
| AutonomyDial.jsx + useAutonomyTier | Autonomy Dial | 顶栏区域选择器旁；state 提升在 AgentPage，无全局 Context |
| RationalePanel.jsx | Explainable Rationale | ReportView 状态条下方，默认折叠，sanitized 轨迹（无 args JSON） |
| AuditTimeline.jsx + useAuditLog | Action Audit & Undo | 轨迹 tab 底部审计分区（不新建 Evidence tab） |
| EscalationCard.jsx | Escalation Pathway | message.escalation 负载条件渲染（后端事件未就绪） |

通用约束：纯展示受控组件、零新三方依赖、只用 `var(--color-*)` token、memo 包裹、
SSE 数据不新增 token 事件 setState。

### 契约偏差登记（诚实展示，不伪造能力）

1. **Confidence 枚举映射**：后端 `confidence_level` 为枚举 high/medium/low
   （backend/agent/schemas.py ConfidenceLevel），v2 规格为数值阈值（≥0.8）。
   映射：high → 绿 ✓；medium/low → 琥珀 ?。不伪造百分比；后端补
   `confidence_score` 数值字段后优先读数值（后续任务）。
2. **Autonomy Dial 设备级持久化**：键 `app_autonomy_tier`（localStorage），
   v2 规格为 per-user；服务端用户设置列为后续 epic。UI 标注「界面偏好」语义，
   执行门控待 agent API autonomy tier 契约（TODO 已写入组件注释）。
3. **Escalation 后端待接入**：SSE 尚无 escalation 事件，组件级交付 + 测试，
   message.escalation 负载就绪即生效。
4. **审计范围**：本期记录导出 PDF（不可逆，意图预览中声明）与清空对话
   （可逆，撤销恢复消息快照）两类前端动作。

### index.css 新增 token

`--color-confidence-high` / `--color-confidence-low`：light（#15803d/#b45309）、
dark（oklch 0.72 0.15 155 / 0.78 0.14 80，取自 v2 frontmatter）、print 三处。
注释全中文规避 finlandBoard 守门的 /Inter/ 断言。

## 四、验证结果

| 验证项 | 结果 |
|---|---|
| designmd lint DESIGN-v2.md | ✅ 0 error / 9 warning（基线持平） |
| npm test（vitest） | ✅ 54/54（NarrativeLayer 32 + apiBase 4 + trustPatterns 18 新增）；审查修复后 57/57（trustPatterns +3 守门） |
| npm run test:node（node:test 317 项） | ✅ 317/317（finlandBoard 守门含内） |
| npm run build | ✅ 构建成功 |
| check_bundle_budget.mjs | ✅ PASS：入口 raw 788.6KB ≤ 850KB（与修复前持平——组件全在 lazy AgentPage chunk，入口零影响） |
| 归档断链复核 | ✅ 仅历史日志残留旧路径引用（按政策不回改） |
| design_tokens.json | ✅ 合法 JSON、Archivo 在、无 Inter |

e2e 视觉快照无需重拍：快照面为 Stage 4/6 漏斗页，本次组件只挂 AgentPage（非快照目标）。

## 五、遗留后续任务

1. 后端 `confidence_score` 数值字段（backend/agent/schemas.py + synthesizer 产出），前端徽章优先读数值
2. Autonomy Dial 服务端 per-user 设置 + agent API 执行门控契约
3. SSE escalation 事件定义与下发（EscalationCard 接线即生效）
4. 审计范围扩展（工作流运行、视图覆盖等更多动作类型）

## 六、文件清单

新增：`web/src/components/agent/{ConfidenceBadge,IntentPreview,AutonomyDial,RationalePanel,AuditTimeline,EscalationCard}.jsx`、
`web/src/components/agent/__tests__/trustPatterns.test.jsx`、本文档

修改：`web/package.json`、`.github/workflows/ci.yml`、`README.md`、`docs/deployment/CICD部署与回滚说明.md`、
`docs/design/DESIGN-v2.md`、`docs/design/design_tokens.json`、根 `DESIGN.md`、
`web/src/index.css`、`web/src/pages/AgentPage.jsx`

移动：`docs/design/DESIGN.md` → `docs/_archive/DESIGN-v1.md`（git mv）

## 七、审查修复（2026-08-24，三视角代码审查后）

### 严重

- **密钥泄露清除**：`.env.docker.example` 中被并行改动写入的真实 Tavily key
  已清除为空占位；请在 Tavily 控制台轮换该 key（真实 key 只放本地 .env）。

### 警告修复

1. **useAuditLog.undo 纯化**（AuditTimeline.jsx）：onUndo 副作用移出 setState
   updater（entriesRef 定位），StrictMode 双重调用不再双发；新增 StrictMode 守门测试。
2. **审计时机修正**：导出审计从“意图确认”移至 ExportPreviewModal 真实导出完成
   （新增 onExported 回调），预览弹窗取消不再虚报“导出已完成”。
3. **审计内存上限**：entries 上限 20 条（最旧淘汰）；undo 成功后释放 onUndo 引用，
   避免消息快照常驻内存。
4. **CI vitest 闭环**：ci.yml frontend job 新增 Run vitest guards 步骤（稳定期非阻断），
   双 runner 收口在流水线层面闭环；部署文档 6.2 节同步步骤与门禁状态（node --test
   自 2026-08-20 起为阻断门禁，属并行“测试债清零”任务有意收紧，已登记归属）。
5. **ComparisonPanel 置信度**：纯文本换为 ConfidenceBadge（showLabel={false}），
   AgentPage 内置信度展示全量统一徽章。
6. **test:node 兼容**：glob 引号化 `"src/lib/**/*.test.js"`，由 Node（≥21）自行解析，
   不再依赖 shell 展开（Windows cmd/PowerShell 兼容）；实测 317/317。
7. **ExportPreviewModal 焦点 effect 拆分**：挂载聚焦/卸载归还空依赖只做一次，
   keydown 监听随 [exporting, onClose] 更新，消除内联 onClose 引发的焦点争抢。

### 建议项落实

- IntentPreview 遮罩补 `role="dialog" aria-modal` + Esc/遮罩点击关闭（对齐批次6 标准）；
- undo_failed 条目展示“撤销失败”标记（不再渲染空字符串）；
- ConfidenceBadge title 未知枚举时标注“回落中档”，与可见文案一致；
- AgentPage 解构 auditLog 稳定引用，消除 exhaustive-deps 告警；
- design_tokens.json 无 components 段为工具行为（权威源为 DESIGN-v2.md frontmatter），
  已在本记录第二节登记，不改生成产物。

### 修复后验证

| 验证项 | 结果 |
|---|---|
| npm test（vitest） | ✅ 57/57（trustPatterns 21，含 StrictMode/上限/撤销失败 3 新守门） |
| npm run test:node | ✅ 317/317（新 glob 写法实测通过） |
| eslint 本次改动文件 | ✅ 无新增 error/warning（AgentPage 余 5 error 为存量 lint 债，CI 非阻断） |
| npm run build + 预算 | ✅ 构建成功，入口 raw 788.6KB PASS |
