# 04 - 模块关系图 / Component Dependency Diagram

> 回答：每个系统内部有哪些核心模块？它们之间怎么依赖？

---

## 后端引擎模块分类（25 个）

### 分类总览

```
┌─────────────────────────────────────────────────────────────────┐
│                     市场适配层                                    │
│  market_adapter.py (NEM/WEM/Finland 统一接口)                    │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                     核心引擎层 (7 类)                             │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│ 电池物理    │ 回测与优化   │ 价格与收入   │ 财务建模            │
│ (2 个)      │ (5 个)       │ (3 个)       │ (4 个)              │
├─────────────┼──────────────┼──────────────┼─────────────────────┤
│ 投资前景    │ 叙事与归因   │ ML 校准      │                     │
│ (4 个)      │ (4 个)       │ (1 个)       │                     │
└─────────────┴──────────────┴──────────────┴─────────────────────┘
```

### 电池物理模型（2 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `battery_model.py` | numpy | dispatch_optimizer, bess_backtest_v1/v2 |
| `degradation_model.py` | numpy | financial_model, co_optimization_engine |

### 回测与调度优化（5 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `dispatch_optimizer.py` | PuLP, battery_model | bess_backtest_v1/v2 |
| `bess_backtest_v1.py` | dispatch_optimizer, database | revenue_routes |
| `bess_backtest_v2.py` | dispatch_optimizer, degradation_model | coopt_routes |
| `co_optimization_engine.py` | PuLP, degradation_model | coopt_routes |
| `p3_dispatch_optimizer.py` | PuLP | outlook_routes |

### 价格与收入分析（3 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `price_analysis_engine.py` | database, numpy | price_routes |
| `revenue_analysis_engine.py` | database, numpy | revenue_routes |
| `revenue_model.py` | price_analysis_engine | investment_routes |

### 财务建模（4 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `financial_model.py` | numpy-financial, degradation_model | investment_routes |
| `cost_structure_engine.py` | — | cost_structure_routes |
| `tax_model.py` | numpy | financial_model |
| `forward_price_engine.py` | ml_calibration_engine, cannibalization_engine | forward_price_routes, outlook_routes |

### 投资前景分析（4 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `cannibalization_engine.py` | numpy | forward_price_engine, outlook_routes |
| `fcas_collapse_engine.py` | numpy | outlook_routes |
| `regional_timing_engine.py` | price_analysis, cannibalization | ranking_routes |
| `merchant_risk_engine.py` | numpy (蒙特卡洛) | outlook_routes |

### 叙事与归因（4 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `narrative_engine.py` | forward_price_engine | narrative_routes |
| `risk_stratification_engine.py` | financial_model | narrative_routes |
| `event_annotation_service.py` | database (JSON) | narrative_routes |
| `cross_validation_service.py` | multiple engines | narrative_routes |

### ML 校准（1 个）

| 模块 | 依赖 | 被依赖 |
|------|------|--------|
| `ml_calibration_engine.py` | LightGBM, pandas, scikit-learn | forward_price_engine |

## 前端模块依赖

```
marketConfig.js (模块注册表)
  ├── MarketPage.jsx
  │     ├── DynamicStage.jsx
  │     │     └── ModuleRenderer.jsx (React.lazy)
  │     │           ├── Stage 1: PriceChartModule, SummaryStatsModule, RegionalRanking
  │     │           ├── Stage 2: SpikeProfitAnalysis
  │     │           ├── Stage 3: SaturationTracker
  │     │           ├── Stage 4: Cannibalization, FcasCollapse, RegionalTiming, MerchantRisk, ForwardSpread
  │     │           ├── Stage 5: CoOptimizedBacktest
  │     │           └── Stage 6: RevenueStratification, CrossValidation, EventAnnotation, Fuel, Network
  │     └── 全局: AssetConfigPanel, AssumptionPanel, NarrativeTooltip
  ├── FinlandPage.jsx
  ├── FingridPage.jsx
  └── DeveloperPortalPage.jsx
```

## 路由 → 引擎映射

| 路由模块 | 调用的引擎 |
|----------|-----------|
| price_routes | price_analysis_engine |
| revenue_routes | revenue_analysis_engine, bess_backtest_v1 |
| investment_routes | financial_model, revenue_model, tax_model |
| fcas_routes | co_optimization_engine (部分) |
| coopt_routes | co_optimization_engine, bess_backtest_v2 |
| spike_routes | price_analysis_engine |
| saturation_routes | cannibalization_engine |
| ranking_routes | regional_timing_engine |
| outlook_routes | forward_price_engine, cannibalization, fcas_collapse, merchant_risk |
| narrative_routes | narrative_engine, risk_stratification, event_annotation, cross_validation |
| forward_price_routes | forward_price_engine |
| cost_structure_routes | cost_structure_engine |
| wem_modules_routes | market_adapter (WEM) |
| finland_routes | market_adapter (Finland) |


