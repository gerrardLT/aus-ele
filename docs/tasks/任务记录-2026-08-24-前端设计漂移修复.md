# 任务记录：前端设计漂移修复

- 日期：2026-08-24
- 类型：实施修复（承接同日《前端多角度评估与设计范式调研》遗留项 1–3）
- 前置文档：`docs/tasks/任务记录-2026-08-24-前端多角度评估与设计范式调研.md`

## 一、方向定案

根 `DESIGN.md` 已于 2026-08-20（v1.1）定案：**Archivo/Source Serif 4 为正式字体、`web/src/index.css` 为运行期 token SSOT**。
因此本次修复方向为**文档向实现对齐**（DESIGN-v2.md 改 Inter→Archivo），而非代码迁就文档。

## 二、修改清单

### 2.1 index.css 违规效果清除（web/src/index.css）
- 删除 `.glow-line`（零引用死代码）、`.glow-kpi`（text-shadow）、`.panel-glass`（backdrop-blur 12px + 暗色 inset）、`.panel-depth`（hover translateY + box-shadow）全部规则
- 删除孤儿变量 `--glow-opacity` / `--glow-color`（light/dark/print 三处，已核实仅被上述 glow 类消费）
- 保留四个 `--color-background/panel/surface/surface-hover` 变量（finlandBoard.test.js L683-686 守门断言）

### 2.2 组件类名清理（7 文件 10 处）
| 文件 | 操作 |
|---|---|
| funnel/KpiCard.jsx | 删 `glow-kpi` |
| modules/ReactiveParamPanel.jsx | 删 `panel-glass` + `glow-kpi` |
| modules/DecisionTerminal.jsx | 删 `panel-glass` ×2 + `glow-kpi` |
| modules/ScenarioSplit.jsx | 删 `panel-glass` |
| AnomalyBadge.jsx | 删 `panel-glass` + `shadow-xl`（同属违反禁投影） |
| funnel/CollapsibleModule.jsx | 删 `panel-glass panel-depth`（按钮已有 `hover:bg-surface-hover` 阶跃，无需补替代） |
| ExportPreviewModal.jsx | 删内联打印样式中 `--glow-*` 两行 |

### 2.3 blur 白名单裁定
- FinlandDataTable.jsx：sticky 表头 `backdrop-blur` → `backdrop-blur-sm`（对齐"一档"规则）
- OnboardingTour.jsx 模态遮罩 blur：**保留**，根 DESIGN.md 白名单新增豁免条款（遮罩非内容面板）
- MarketPage.jsx sticky tab blur 合规，不动
- 白名单政策同步迁入 DESIGN-v2.md Do's and Don'ts（内容面板禁 blur / sticky 一档 / 模态遮罩一档）

### 2.4 字体 SSOT 对齐
- DESIGN-v2.md：frontmatter 6 处 + 正文 `Inter` → `Archivo`；过时 migration note 改写为"Archivo 为已批准 sans 栈（根 DESIGN.md v1.1），Inter 弃用"
- design_tokens.json：6 处 `"fontFamily": "Inter"` → `"Archivo"`（最小对齐）
- HourlyDistributionChart.jsx：硬编码 `'Archivo...'` → `'var(--font-sans)'`（对齐 PriceChart 写法）

### 2.5 规格文档治理
- docs/design/DESIGN.md（v1）头部加 superseded 标记指向 DESIGN-v2.md + 根 DESIGN.md（**不物理归档**，v2 仍 alpha）
- 根 DESIGN.md：白名单补两条（sticky 表头同类、模态遮罩豁免），版本记录加 v1.2 行

## 三、验证结果

| 验证项 | 结果 |
|---|---|
| grep 残留（glow-kpi/glow-line/panel-glass/panel-depth/--glow-） | ✅ 零残留（仅 index.css 注释提及） |
| `designmd lint DESIGN-v2.md` | ✅ 0 error / 9 warning（orphaned-tokens，与修复前持平） |
| vitest run（组件测试） | ✅ 36/36 通过 |
| node --test（lib + test，318 项） | ⚠️ 317 通过 / 1 失败 |
| npm run build | ✅ 构建成功（31.9s） |

**唯一失败项说明**：`web/src/test/apiBase.test.js`（untracked 新增文件）在 node:test runner 下
因引入 vitest `describe` 报 `Cannot read properties of undefined (reading 'config')`。
该文件属并行开发中的新增测试（runner 选择错误，应归 vitest 侧或改用 node:test API），
**与本次修复无关**——本次触碰的全部文件对应测试（finlandBoard 等）全绿。

## 四、推迟项（2026-08-24 更新：项 1/2/3/5 已由《任务记录-2026-08-24-V2转正与信任组件落地.md》承接关闭）

1. **token 双源合一**：`designmd export --format dtcg` 重生成 design_tokens.json，待 DESIGN-v2.md 转正后执行
2. **v1 物理归档**：待 v2 经用户确认转正后移入 `docs/_archive/`
3. **v2 信任组件落地**：意图预览 / 置信度徽章 / 自主性拨盘 / 审计撤销——功能开发项，单独立任务
4. **bundle 预算口径澄清**：`web/scripts/check_bundle_budget.mjs` 实测基线为 850KB raw（非 300KB）；
   300KB 目标依赖 recharts 组件级懒加载，属后续 epic；且该脚本尚未挂入 npm scripts/CI
5. **apiBase.test.js runner 修正**：建议移交该文件作者处理（vitest 套件或改 node:test API）

## 五、视觉影响说明

- 暗色模式 KPI 数字失去 text glow、面板失去 backdrop-blur/投影——均为规格预期行为
  （DESIGN-v2.md：flat tonal layering）；所有面板已有实色背景 + hairline border，层级表达不受影响
- 若 e2e 视觉快照（tests/e2e/narrative_layer_visual.spec.js）超容差，属预期视觉变更，
  按流程声明后更新快照即可
